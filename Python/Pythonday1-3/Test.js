var current_tester = [1, 2, a, b, 3, n, 6, 8, a]
var current_type = current_tester

{
    if (current_type is str: )
        console.log(Its a baby string)
    if (current_type is int: )
        console.log(Its a number)
    else;
    console.log(Dawg not cool)

    return (current_type)
}